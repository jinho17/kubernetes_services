apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  annotations:
    metallb.universe.tf/allow-shared-ip: ft_services
  labels:
    app: wordpress-app
spec:
  ports:
    - name: wordpress
      port: 5050
      targetPort: 5050
  selector:
    app: wordpress-app
  type: LoadBalancer