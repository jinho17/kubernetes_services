apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin-service
  annotations:
    metallb.universe.tf/allow-shared-ip: ft_services
spec:
  ports:
    - name: phpmyadmin
      targetPort: 5000
      port: 5000
  selector:
    app: phpmyadmin-app
  type: LoadBalancer